## Subqueries
SELECT query which runs within another query (needs to be select??)
##### Correlated
- References the outer query from within the subquery. 
- Needs to do a comparison for every row
##### Uncorrelated
- Does not reference the outer query
- The subquery runs once, independently