## Sockets
- Abstraction that allows communication between processes on the same machine or over a network
- Software construct that wraps a combination of Protocol (TCP/UDP), Port Number, and IP address